# دليل النشر على Hostinger

## نظرة عامة
هذا الدليل يشرح خطوات نشر ملف الإنجاز الإلكتروني على استضافة Hostinger خطوة بخطوة.

---

## المتطلبات الأولية

1. ✅ حساب Hostinger نشط
2. ✅ خطة استضافة تدعم Node.js أو استضافة ويب عادية
3. ✅ الوصول إلى File Manager أو FTP
4. ✅ المشروع جاهز على جهازك المحلي

---

## الخطوة 1: بناء المشروع

### 1.1 التأكد من تثبيت الحزم

افتح Terminal/Command Prompt في مجلد المشروع وقم بتشغيل:

```bash
npm install
```

### 1.2 بناء المشروع للإنتاج

```bash
npm run build
```

هذا الأمر سينشئ مجلد `out` يحتوي على جميع الملفات الثابتة الجاهزة للنشر.

### 1.3 التحقق من البناء

تأكد من وجود مجلد `out` في مجلد المشروع. يجب أن يحتوي على:
- ملفات HTML
- مجلد `_next`
- مجلد `hasoob` (الملفات الأصلية)

---

## الخطوة 2: الاتصال بـ Hostinger

### الطريقة الأولى: File Manager (موصى بها)

1. سجل الدخول إلى لوحة تحكم Hostinger
2. اذهب إلى **Files** → **File Manager**
3. انتقل إلى مجلد `public_html` (أو المجلد الجذر لموقعك)

### الطريقة الثانية: FTP

استخدم برنامج FTP مثل FileZilla:
1. في لوحة تحكم Hostinger، اذهب إلى **Files** → **FTP Accounts**
2. احصل على بيانات FTP:
   - Host/Server
   - Username
   - Password
   - Port (عادة 21)
3. اتصل عبر FileZilla

---

## الخطوة 3: رفع الملفات

### 3.1 تنظيف المجلد الجذر (اختياري)

إذا كان هناك ملفات قديمة في `public_html`، قم بحذفها أو نقلها.

### 3.2 رفع محتويات مجلد out

**مهم جداً:** لا ترفع مجلد `out` نفسه، بل محتوياته فقط.

#### عبر File Manager:
1. افتح مجلد `out` على جهازك
2. حدد جميع الملفات والمجلدات داخله (Ctrl+A أو Cmd+A)
3. اسحبها وأفلتها في File Manager داخل `public_html`
4. انتظر حتى تكتمل عملية الرفع

#### عبر FTP:
1. في FileZilla، افتح مجلد `out` المحلي (الجهة اليسرى)
2. حدد جميع محتوياته
3. اسحبها إلى `public_html` (الجهة اليمنى)
4. انتظر حتى تكتمل عملية الرفع

---

## الخطوة 4: التحقق من الهيكل

بعد الرفع، يجب أن يبدو هيكل `public_html` كالتالي:

```
public_html/
├── index.html
├── 404.html
├── intro-video.html
├── reflections.html
├── cv.html
├── philosophy.html
├── teaching-method.html
├── webquest.html
├── resources.html
├── iste.html
├── _next/
│   ├── static/
│   └── ...
└── hasoob/
    ├── video/
    ├── reflections/
    ├── cv/
    ├── philosophy/
    ├── teaching_method/
    ├── webquest/
    └── references/
```

---

## الخطوة 5: إعداد ملف .htaccess (مهم جداً)

Next.js يولد صفحات بامتداد `.html`، لكننا نريد روابط نظيفة بدون الامتداد.

### 5.1 إنشاء ملف .htaccess

في مجلد `public_html`، أنشئ ملف جديد باسم `.htaccess` (لاحظ النقطة في البداية).

### 5.2 إضافة الكود التالي:

```apache
# تمكين الروابط النظيفة
RewriteEngine On

# إزالة .html من الروابط
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.+)$ $1.html [L,QSA]

# إعادة توجيه من .html إلى بدون امتداد
RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s([^.]+)\.html [NC]
RewriteRule ^ %1 [R=301,L]

# تعيين صفحة 404 مخصصة
ErrorDocument 404 /404.html

# تمكين الضغط
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript
</IfModule>

# تمكين التخزين المؤقت
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
</IfModule>

# UTF-8 Encoding
AddDefaultCharset UTF-8

# MIME Types
AddType text/html .html
AddType application/javascript .js
AddType text/css .css
```

احفظ الملف.

---

## الخطوة 6: رفع ملفات hasoob

### 6.1 تحضير الملفات الأصلية

تأكد من وجود جميع الملفات التالية على جهازك:

- `hasoob/video/intro.mp4`
- `hasoob/reflections/reflection_01.docx` (وباقي التفكيرات)
- `hasoob/cv/cv.pdf`
- `hasoob/philosophy/philosophy.docx`
- `hasoob/teaching_method/preferred_method.pdf`
- `hasoob/webquest/teacher_page.pdf`
- `hasoob/webquest/student_page.pdf`
- `hasoob/references/iste.pdf`

### 6.2 رفع الملفات

إذا لم يتم رفع مجلد `hasoob` تلقائياً ضمن `out`:

1. في File Manager أو FTP، انتقل إلى `public_html`
2. أنشئ مجلد `hasoob` إذا لم يكن موجوداً
3. أنشئ المجلدات الفرعية: `video`, `reflections`, `cv`, إلخ
4. ارفع الملفات المناسبة لكل مجلد

---

## الخطوة 7: ربط النطاق (Domain)

### 7.1 إذا كنت تستخدم نطاقاً جديداً:

1. في لوحة تحكم Hostinger، اذهب إلى **Domains**
2. أضف النطاق الخاص بك
3. وجهه إلى `public_html` (المجلد الذي رفعت فيه الملفات)

### 7.2 إذا كنت تستخدم Subdomain:

1. أنشئ Subdomain (مثل `portfolio.yoursite.com`)
2. وجهه إلى المجلد المناسب

---

## الخطوة 8: تفعيل SSL (HTTPS)

1. في لوحة تحكم Hostinger، اذهب إلى **SSL**
2. فعّل SSL المجاني من Let's Encrypt
3. انتظر بضع دقائق حتى يتم التفعيل
4. فعّل "Force HTTPS" لإعادة توجيه جميع الزوار إلى HTTPS

---

## الخطوة 9: الاختبار النهائي

### 9.1 اختبار الصفحات

زر الروابط التالية (استبدل `yourdomain.com` بنطاقك):

- `https://yourdomain.com/` (الرئيسية)
- `https://yourdomain.com/intro-video`
- `https://yourdomain.com/reflections`
- `https://yourdomain.com/cv`
- `https://yourdomain.com/philosophy`
- `https://yourdomain.com/teaching-method`
- `https://yourdomain.com/webquest`
- `https://yourdomain.com/resources`
- `https://yourdomain.com/iste`

### 9.2 اختبار التحميلات

جرب الضغط على أزرار "تحميل الملف الأصلي" في كل صفحة للتأكد من:
- عمل روابط التحميل
- تحميل الملف الصحيح

### 9.3 اختبار الفيديو

في صفحة "فيديو تعريفي"، تأكد من:
- عرض الفيديو بشكل صحيح
- عمل أزرار التحكم
- إمكانية تحميل الفيديو

### 9.4 اختبار الموبايل

افتح الموقع من هاتفك المحمول وتأكد من:
- ظهور القائمة المنسدلة (hamburger menu)
- عمل جميع الروابط
- سهولة القراءة

---

## حل المشاكل الشائعة

### المشكلة 1: الصفحات تعرض 404

**الحل:**
- تأكد من وجود ملف `.htaccess` في `public_html`
- تحقق من أن ملفات `.html` موجودة (مثل `reflections.html`)

### المشكلة 2: الروابط تحتوي على .html

**الحل:**
- راجع إعدادات `.htaccess`
- أعد تحميل الصفحة مع مسح الكاش (Ctrl+Shift+R)

### المشكلة 3: الملفات لا تُحمّل من hasoob

**الحل:**
- تحقق من صلاحيات المجلد (يجب أن تكون 755)
- تأكد من الأسماء الصحيحة (حساسة لحالة الأحرف)
- تحقق من المسارات في الكود

### المشكلة 4: الخطوط العربية لا تظهر بشكل صحيح

**الحل:**
- تأكد من إضافة `UTF-8` في `.htaccess`
- امسح الكاش وأعد تحميل الصفحة

### المشكلة 5: الصور أو الأيقونات مفقودة

**الحل:**
- تأكد من رفع مجلد `_next` كاملاً
- تحقق من روابط الصور في الكود

---

## التحديثات المستقبلية

### لتحديث المحتوى:

1. عدّل الملفات على جهازك المحلي
2. قم ببناء المشروع مجدداً: `npm run build`
3. احذف الملفات القديمة من `public_html`
4. ارفع محتويات `out` الجديدة
5. احتفظ بملف `.htaccess` (لا تحذفه)

---

## نصائح إضافية

✅ **النسخ الاحتياطي:** احتفظ بنسخة احتياطية من الملفات قبل أي تحديث
✅ **الأداء:** استخدم Cloudflare لتحسين السرعة (اختياري)
✅ **المراقبة:** راقب زوار الموقع عبر Google Analytics (اختياري)
✅ **التوثيق:** احتفظ بنسخة من هذا الدليل للرجوع إليه

---

## الدعم

إذا واجهت أي مشاكل:
1. راجع قسم "حل المشاكل الشائعة" أعلاه
2. تواصل مع دعم Hostinger (متوفر 24/7)
3. راجع توثيق Next.js الرسمي: https://nextjs.org/docs

---

**تهانينا! 🎉**

ملف إنجازك الإلكتروني الآن منشور ويمكن الوصول إليه من أي مكان في العالم!
